<configuration debug="false">

    <!-- Console appender (for live debugging) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%highlight(%-5level) %cyan(%logger{15}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- General application log -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/javalin-app.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Debug log for your own app -->
    <appender name="DEBUGFILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/debug.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress framework spam -->
    <logger name="org.hibernate" level="WARN" />
    <logger name="org.eclipse.jetty" level="WARN" />
    <logger name="io.javalin" level="INFO" />
    <logger name="com.zaxxer.hikari" level="WARN" />

    <!-- Your application package logs -->
    <logger name="app" level="DEBUG" additivity="false">
        <appender-ref ref="DEBUGFILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger (fallback for all) -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
